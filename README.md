# Pinball Game

HTML5 Canvas と Matter.js（ローカル同梱）で作ったブラウザ向けピンボールゲームです。

## 起動

```bash
npm install
npm run dev
```

`http://localhost:5173` を開くと遊べます。

## 実装ステップ

1. 最小構成: ボール・壁・フリッパーで遊べる状態を構築。
2. 機能追加: バンパー衝突で加点、残機表示、ゲームオーバー表示を追加。

## 操作

- `Space`: ボール発射
- `←` / `→`: 左右フリッパー
